Crear una funcion que reciba dos numeros y devuelva el mayor de ellos.

Crea un funcion que reciba un numero y devueva si es par o impar.

Crea una funcion que reciba tres numeros como parametros y devuelva su promedio.

Crea una funcion que calcula la longitud de una palabra si es corta o larga.

Crea una funcion para calcular cuantas silabas tiene cada palabra e imprimir el resultado.

Crea una funcion que genere la tabla de multiplicar de n, pero que no se muestre la multiplicacion x5.

Crea una funcion que devuelva el elemento mayor de un arreglo de numeros.

Crea una funcion que tome una cade como parametro y devuelva la cadena en orden inverso, por ejemplo, si la cadena de entrada es "hola", la funcion deberia devolver "aloh".

Crea una funcion para validar un pin que acepta 4 o 6 caracteres de longitud, solo numeros, la funcion debe devolver true si el pin es valido o false si no es.

Genera unpatron de asteriscos en forma de piramide dependiendo una cantidad de filas.

Obtiene una serie de numeros y devuelve la suma de todos los positivos. si no hay nada que sumar, la suma predeterminada es 0.

Implemente una funcion que sume dos numeros y devuelva su suma en binario, el numero binario devuelvo debe ser una cadena.

Escriba una funcion que tome una cadena y devuelva una nueva cadena sin todas las vocales.

Crea una funcion que tome un conjunto de numeros y devuelve el inverso aditivo de cada uno, todo lo positivo se vuelve negativo y lo negativo a positivo".

Escribe una funcion que haga de forma aleatoria un numero entre 1 a 10 y verifica si es numero par o impar.